apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rook-ingress
  namespace: teamwald
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: teamwald.local
    http:
      paths:
      - path: /ws
        pathType: Prefix
        backend:
          service:
            name: rook
            port:
              number: 9090
